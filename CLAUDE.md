# UnderCooked — 작업 규칙

작업을 시작하기 전에 `.claude/docs/` 안의 문서를 먼저 읽는다.

| 문서 | 내용 |
|---|---|
| `.claude/docs/BRANCHING.md` | 브랜치 전략 (`dev_<작업명>` → `dev` → `main`, PR 규칙) |

새 문서가 `.claude/docs/`에 추가되면 이 표에도 같이 추가한다.
